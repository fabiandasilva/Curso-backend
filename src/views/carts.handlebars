<h1>Cart</h1>

<div class="space-y-4">
    {{#each carts}}
    <div class="border rounded-lg p-4">
        {{#each this.products}}
        <p>Product ID: {{this.productId}}</p>
        <p>Quantity: {{this.quantity}}</p>
        {{/each}}
    </div>
    {{/each}}
</div>